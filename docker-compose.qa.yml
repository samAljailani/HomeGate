services:
  server:
    container_name: homegate-qa-server
    build:
      context: .
      dockerfile: deploy/Dockerfile
      args:
        PORT: ${PORT:-3004}
    restart: unless-stopped
    environment:
      APP_ENV: development
      PORT: ${PORT:-3004}
      HOST: https://${APP_SUBDOMAIN}.${DOMAIN}
      CLIENT_RELATIVE_STATIC_PATH: ./dist/client/
      SESSION_SECRET: ${SESSION_SECRET}
      SESSION_COOKIE_NAME: HOMEGATE_QA_SID
      SESSION_COOKIE_DOMAIN: ${SESSION_COOKIE_DOMAIN:-.${DOMAIN}}
      GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}
      GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET}
      DATABASE_URL: ${DATABASE_URL}
      LOG_TARGETS: console,database
      LOG_LEVEL: debug
      LOG_FORMAT: console
      COLOR_LOGS: "false"
    ports:
      - "${PORT:-3004}:${PORT:-3004}"
    depends_on:
      db:
        condition: service_healthy
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.homegate-qa.entrypoints=websecure"
      - "traefik.http.routers.homegate-qa.rule=Host(`${APP_SUBDOMAIN}.${DOMAIN}`)"
      - "traefik.http.routers.homegate-qa.tls.certresolver=sslresolver"
      - "traefik.http.services.homegate-qa.loadbalancer.server.port=${PORT:-3004}"
      - "traefik.http.middlewares.homegate-qa-forward-auth.forwardauth.address=http://homegate-qa-server:${PORT:-3004}/api/auth/forward"
      - "traefik.http.middlewares.homegate-qa-forward-auth.forwardauth.trustForwardHeader=true"
    networks:
      - traefik

  db:
    container_name: homegate-qa-db
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB:-HomeGate_QA}
      TZ: America/New_York
      PGTZ: America/New_York
    command: ["postgres", "-c", "timezone=America/New_York"]
    volumes:
      - homegate-qa-pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 5s
      timeout: 3s
      retries: 5
      start_period: 60s
    networks:
      - traefik
  adminer:
    container_name: homegate-qa-adminer
    image: adminer
    restart: always
    environment:
      TZ: America/New_York
    ports:
      - "8754:8080"
    depends_on:
      - db
    networks:
      - traefik
volumes:
  homegate-qa-pgdata:

networks:
  traefik:
    external: true
    name: traefik_default
